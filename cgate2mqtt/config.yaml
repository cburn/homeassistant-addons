name: "CGate to MQTT"
description: "Clipsal CBUS to MQTT via C-Gate"
version: "0.1.10"
slug: "cgate2mqtt"
init: false
arch:
  - aarch64
  - amd64
  - armhf
  - armv7
  - i386
services:
  - mqtt:need
options:
  cgate_host:
schema:
  cgate_host: "str"
  mqtt_host: "str?"
  mqtt_port: "str?"
  mqtt_user: "str?"
  mqtt_pass: "str?"
